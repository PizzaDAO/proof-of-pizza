"use client";

// Admin providers - simplified since we no longer need wallet connection
// Keeping this file for potential future use (e.g., adding React Query for data fetching)

export function AdminProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
